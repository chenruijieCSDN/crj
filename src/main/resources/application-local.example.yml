# 复制为 application-local.yml 并填入真实配置（application-local.yml 已加入 .gitignore，不会提交）
spring:
  ai:
    dashscope:
      api-key: your-dashscope-api-key
  datasource:
    url: jdbc:postgresql://your-host:5432/your_db
    username: your_user
    password: your_password

# SearchAPI.io 网页搜索 Key，智能体 searchWeb 需要，注册后在此填入 API Key
search-api:
  api-key: your-searchapi-key

amap:
  web-api-key: your-amap-web-key
